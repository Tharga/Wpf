# Feature: Icon, docs site, and NuGet upgrades

## Source
Two pending requests in `$DOC_ROOT/Tharga/Requests.md`:
- "Move PackageIconUrl to thargelion.net/assets" → Tharga.Wpf (line 1682)
- "Documentation sites under tharga.net" → Tharga.Wpf (line 1802)

Also bundled (user opted in 2026-06-08):
- NuGet upgrades — Tharga.Runtime 0.1.12 → 0.1.14, Velopack 0.0.1298 → 1.2.0, Microsoft.NET.Test.Sdk 18.5.1 → 18.6.0

Mirrors the `feature/icon-and-docs` branch pattern already used by Tharga.Test, Tharga.Mcp, Tharga.Runtime, Tharga.Communication, Tharga.Reporter, Tharga.Console.

## Goal
- Replace the WordPress-hosted `<PackageIconUrl>` (both `Tharga.Wpf` and `Tharga.License`) with the clean `https://thargelion.net/assets/component-wpf.png`. Both packages share the same icon (matches the `Tharga.Communication` two-package pattern; no `component-license.png` asset exists).
- Stand up a DocFX-generated documentation site at `wpf.tharga.net`, modeled on Tharga.Test's `docs/` layout (option (b) — absolute logo URL + `_master.tmpl` overlay).
- Bump outdated NuGet packages. Velopack 0.x → 1.x is a major API change and may require code adjustments in `VelopackApplicationUpdateStateService`.
- Wire the docs build/deploy into the existing `build.yml` so it runs only after `release` succeeds — never as a separate `docs.yml`.

## Scope

**Source / packages**
- `Tharga.Wpf/Tharga.Wpf.csproj` — `<PackageIconUrl>` → `https://thargelion.net/assets/component-wpf.png`
- `Tharga.License/Tharga.License.csproj` — `<PackageIconUrl>` → `https://thargelion.net/assets/component-wpf.png`
- `Tharga.Wpf/Tharga.Wpf.csproj` — Tharga.Runtime 0.1.12 → 0.1.14, Velopack 0.0.1298 → 1.2.0
- `Tharga.License.Tests/`, `Tharga.Wpf.Tests/` — Microsoft.NET.Test.Sdk 18.5.1 → 18.6.0
- Fix any compile/test breakage from Velopack 1.x.

**Documentation site (`docs/`)** — Test pattern (option (b)):
- `docs/docfx.json` — metadata sources = both `Tharga.Wpf.csproj` and `Tharga.License.csproj`, absolute logo path `https://thargelion.net/assets/component-wpf.png`, custom template `thg`
- `docs/CNAME` = `wpf.tharga.net`
- `docs/index.md` — landing page
- `docs/toc.yml` — Home / Articles / API
- `docs/articles/index.md` + `toc.yml`
- 8 articles covering the main README sections:
  - `getting-started.md`
  - `application-base.md` — `ApplicationBase`, IOC, MVVM binding helper
  - `single-instance.md` — single-instance lock, Velopack release behavior
  - `window-management.md` — close behavior, startup state, location persistence
  - `tab-navigator.md` — TabView, OpenTabCommand, AllowMultiple/AllowClose/AllowTabsWithSameTitles
  - `splash.md` — triggering, built-in images (light + dark), foreground color, custom image, custom ISplash
  - `application-update.md` — Velopack, update intervals, splash behavior during update
  - `exception-handling.md` — IExceptionHandler<T>, ErrorId dialog, AI logging
  - `license-server.md` — Tharga.License integration
- `docs/templates/thg/public/main.css` — logo-size override (mirrors Test)
- `docs/templates/thg/layout/_master.tmpl` — absolute-URL overlay (mirrors Test)
- `docs/api/` — generated by DocFX, gitignored
- `.gitignore` — exclude `docs/_site/`, `docs/api/`

**README**
- Add `**Docs:** [wpf.tharga.net](https://wpf.tharga.net)` line near the top.

**CI workflow (`.github/workflows/build.yml`)** — Test reference:
- Add `pages: write` + `id-token: write` to workflow `permissions`
- Append `docs` job (`needs: release`, runs on `ubuntu-latest`, `docfx docs/docfx.json`, uploads Pages artifact)
- Append `docs-deploy` job (`needs: docs`, `concurrency: pages`, `environment: github-pages`, `actions/deploy-pages@v4`)
- Do NOT add a separate `docs.yml`.

## Out of scope (manual follow-ups in GitHub / DNS UI)
- DNS: `CNAME wpf.tharga.net → tharga.github.io`
- Repo Settings → Pages → Source = "GitHub Actions"
- `github-pages` environment (typically auto-created on first deploy)

## Acceptance criteria
- `dotnet build -c Release` clean
- All tests pass after Velopack 1.x bump
- `docfx docs/docfx.json` builds locally without errors
- PR opens against `master`, workflow runs green
- After merge: stable release `2.2.0` published (minor bump — Velopack 0.x → 1.x is a transitive breaking change for any consumer that touches Velopack directly), docs deployed at `wpf.tharga.net`

## Done condition
User confirms the docs site is reachable and the two Requests.md items can be marked Done.
